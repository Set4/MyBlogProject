@using WebBlog.DataAccess.ViewModel
@model List<PostView>
@{
  
}
<div>
    @if (Model.Count > 0)
    {
     @foreach (PostView item in Model)
     {
        @Html.Partial("_ViewPost", new { item })
     }
    }
    else
    {
        <!--elsi postov net-->
    }
</div>
<pioneer-pagination info="@ViewBag.PaginatedMeta" route="/blog"></pioneer-pagination>